name: Deploy Container Registry

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (dev/uat/sbx/prd)'
        required: true
        type: choice
        options: 
          - dev
          - uat
          - sbx
          - prd
        default: dev
      skip_whatif:
        description: "Skip what-if analysis"
        required: false
        default: false
        type: boolean

jobs:
  deploy-acr:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ github.event.inputs.environment == 'dev' && secrets.AZURE_CREDENTIALS_DEV || github.event.inputs.environment == 'uat' && secrets.AZURE_CREDENTIALS_UAT || github.event.inputs.environment == 'sbx' && secrets.AZURE_CREDENTIALS_SBX || secrets.AZURE_CREDENTIALS_PRD }}
      
      - name: Determine Resource Group name
        id: set-rg
        run: |
          ENV="${{ github.event.inputs.environment }}"
          if [ "$ENV" = "dev" ]; then 
            RG_NAME="${{ secrets.RESOURCE_GROUP_DEV }}"
          elif [ "$ENV" = "uat" ]; then 
            RG_NAME="${{ secrets.RESOURCE_GROUP_UAT }}"
          elif [ "$ENV" = "sbx" ]; then 
            RG_NAME="${{ secrets.RESOURCE_GROUP_SBX }}"
          elif [ "$ENV" = "prd" ]; then 
            RG_NAME="${{ secrets.RESOURCE_GROUP_PRD }}"
          fi
          echo "rgName=$RG_NAME" >> "$GITHUB_OUTPUT"
          echo "Deploying to Resource Group: $RG_NAME"
        shell: bash
      
      - name: Bicep Lint
        run: |
          echo "::group::Running Bicep Linter"
          az bicep lint --file infra/container-registry/container-registry.bicep
          echo "::endgroup::"
      
      - name: Validate Bicep template
        run: |
          echo "::group::Building Bicep Template"
          az bicep build --file infra/container-registry/container-registry.bicep
          echo "::endgroup::"
      
      - name: What-If Analysis
        if: ${{ !inputs.skip_whatif }}
        run: |
          echo "::group::Running What-If Analysis"
          az deployment group what-if \
            --resource-group "${{ steps.set-rg.outputs.rgName }}" \
            --template-file infra/container-registry/container-registry.bicep \
            --parameters @"infra/container-registry/container-registry-${{ github.event.inputs.environment }}.parameters.json" \
            --result-format FullResourcePayloads
          echo "::endgroup::"
      
      - name: Deploy Container Registry
        id: deploy-acr
        run: |
          echo "::group::Deploying Container Registry"
          DEPLOYMENT_OUTPUT=$(az deployment group create \
            --resource-group "${{ steps.set-rg.outputs.rgName }}" \
            --template-file infra/container-registry/container-registry.bicep \
            --parameters @"infra/container-registry/container-registry-${{ github.event.inputs.environment }}.parameters.json" \
            --query 'properties.outputs.acrName.value' \
            --output tsv \
            --verbose)
          
          echo "acrName=$DEPLOYMENT_OUTPUT" >> "$GITHUB_OUTPUT"
          echo "ACR deployed: $DEPLOYMENT_OUTPUT"
          echo "::endgroup::"
      
      - name: Display Deployment Summary
        run: |
          echo "### Deployment Summary " >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | ${{ github.event.inputs.environment }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Resource Group** | ${{ steps.set-rg.outputs.rgName }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **ACR Name** | ${{ steps.deploy-acr.outputs.acrName }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **What-If Run** | ${{ inputs.skip_whatif && '❌ Skipped' || '✅ Executed' }} |" >> $GITHUB_STEP_SUMMARY